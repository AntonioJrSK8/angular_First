import { QuantEixoDirective } from './quant-eixo.directive';

describe('QuantEixoDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantEixoDirective();
    expect(directive).toBeTruthy();
  });
});
